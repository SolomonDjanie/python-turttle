import turtle, random


turtle.colormode(1)

win = turtle.Screen()

win.bgcolor(0 ,0 ,0)

turtle.speed(0)

turtle.width(1)
def squ ():
    for k in range(200):
        
       turtle.dot( k)
for k in range (12):
     turtle.color(random.uniform (0 , 1),random.uniform (0 , 1),random.uniform (0 , 1))
     turtle.dot(random)
     
     
     
